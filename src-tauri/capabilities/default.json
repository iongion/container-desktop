{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "shell:default",
    "core:app:default",
    "core:event:default",
    "core:window:default",
    "core:menu:default",
    "core:image:default",
    "core:path:default",
    "core:tray:default"
  ]
}
