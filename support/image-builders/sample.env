# Sample env_file for the compose dev stack — merged into the api service's runtime environment
# during load (inline `environment:` keys win over these). Only used by the Stacks dev sample.
API_TOKEN=dev-sample-token
LOG_LEVEL=debug
